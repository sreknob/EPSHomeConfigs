---
wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
  reboot_timeout: ${reboot_timeout}
  use_address: ${static_address}
  ap:
    ssid: ${device}
    password: !secret fallback_password
    ap_timeout: 5min
captive_portal:
